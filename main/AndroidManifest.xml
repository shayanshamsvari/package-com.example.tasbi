<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tasbi">

    <!-- مجوزها -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/icon"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:name="com.example.tasbi.MyApplication"
        android:theme="@style/Theme.Tasbi">

        <!-- اکتیویتی‌های داخلی (فقط از داخل اپ قابل دسترسی) -->
        <activity
            android:name=".backgroundshop"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".ShopActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".Ringshop"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- MainActivity داخلی است و لانچر نیست -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".Tormajazi"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- صفحه‌ای که بعد از اسپلش باز می‌شود -->
        <activity
            android:name=".ChangeActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- SplashActivity: تنها لانچر اپ -->
        <activity
            android:name=".splashactivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
